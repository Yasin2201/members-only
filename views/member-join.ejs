<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title %>
    </title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <% if (user) {%>

        <nav>
            <h1>
                Members Only
            </h1>

            <div>
                <a href="/">Home</a>
                <a href="/new-message">Create New Message</a>
                <a href="/member-join">Become a Member</a>
                <a href="/admin-access">Become a Admin</a>
                <a href="/log-out">Log Out</a>
            </div>
        </nav>

        <form action="" method="POST">
            <h2>
                <%= title %>
            </h2>
            <p>Enter the SECRET members password to become a club member:</p>

            <input type="text" name="memberPassword" placeholder="Password">
            <button>Join</button>
        </form>

        <% if (typeof errors !='undefined' ) { %>
            <ul>
                <% errors.forEach(err=> { %>
                    <li>
                        <%= err.msg %>
                    </li>
                    <% }) %>
            </ul>
            <% } %>

                <% } else { %>
                    <p>
                        <a href="/login">Login</a>
                        to see this page.
                    </p>
                    <%}%>
</body>

</html>